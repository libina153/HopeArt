<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hopeart">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HopeArt">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.HopeArt.NoActionBar" />

        <activity
            android:name=".Activity.ActivitySplash"
            android:theme="@style/Theme.HopeArt.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activity.ActivityLogIn"
            android:label="@string/app_name"
            android:theme="@style/Theme.HopeArt.NoActionBar" >

        </activity>

        <activity
            android:name=".Activity.ActivityRegistration"
            android:label="@string/app_name"
            android:theme="@style/Theme.HopeArt.NoActionBar" />

        <activity android:name=".Activity.ActivityForgetPass"/>

        <activity android:name=".Activity.ArtistAddProfile" >
        </activity>

        <activity android:name=".Activity.CustAddProfile" >
        </activity>

        <activity android:name=".Activity.ArtistHomeBottomNav"
            android:theme="@style/Theme.HopeArt" >


        </activity>

    <activity
        android:name=".Activity.CustHomeDrawer"
        android:label="@string/title_activity_cust_drawer"
        android:theme="@style/Theme.HopeArt.NoActionBar">

    </activity>

        <activity android:name=".Activity.ArtistArtworkDetailActivity"/>

        <activity android:name=".Activity.ArtistCustomDetailActivity"/>

        <activity android:name=".Activity.ArtworkHomeDetailsActivity"
            android:theme="@style/Theme.HopeArt"/>

        <activity android:name=".Activity.CustPlaceOrderActivity"
            android:theme="@style/Theme.HopeArt">

        </activity>

    </application>

</manifest>